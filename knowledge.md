# Package Manager

Use `yarn` as the package manager for this project, not npm. For example:
- Use `yarn test` instead of `npm test`
- Use `yarn add` instead of `npm install`
- Use `yarn` instead of `npm install` to install dependencies

