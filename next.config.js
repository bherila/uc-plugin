/** @type {import('next').NextConfig} */
const nextConfig = {
  modularizeImports: {},
  reactStrictMode: false,
  trailingSlash: true,
  compiler: {
    styledComponents: true,
  },
}

module.exports = nextConfig
